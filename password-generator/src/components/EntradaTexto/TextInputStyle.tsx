import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  inputStyle: {
    width: '100%',
    borderWidth: 2,
    borderColor: 'black',
    borderRadius: 5,
    padding: 10,
    marginBottom: 10,
    color: 'White',
    fontSize: 22,
    textAlign: 'center',
  }
});