import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  imageStyle: {
    height: 180,
    resizeMode: 'contain',
  },
  textStyle: {
    textAlign: 'center',
    fontSize: 30,
    fontWeight: 'bold',
    color: '#E5BF3C'
  }
});